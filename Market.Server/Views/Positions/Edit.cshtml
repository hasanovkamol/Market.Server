@model Data.Entity.Position

@{
    ViewData["Title"] = "Edit";
}

<div class="col-md-8">
    <div class="card card-block sameheight-item">
        <div class="title-block">
            <div class="header-block">
                <h3 class="title">@ViewData["Title"]</h3>
            </div>
        </div>
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="Pasition_Name" class="control-label"></label>
                <input asp-for="Pasition_Name" class="form-control" />
                <span asp-validation-for="Pasition_Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a class="btn btn-info-outline" asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
